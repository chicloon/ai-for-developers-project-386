DATABASE_URL=postgres://postgres:postgres@localhost:5432/call_booking?sslmode=disable
# Production (Supabase): прямое подключение — только IPv6 (на Render обычно не подходит):
# DATABASE_URL=postgresql://postgres:<db-password>@db.<project-ref>.supabase.co:5432/postgres?sslmode=require
# Production (Supabase) на IPv4 (Render, GitHub Actions и т.п.) — Session pooler в Dashboard → Connect → Session mode:
# DATABASE_URL=postgresql://postgres.<project-ref>:<db-password>@aws-0-<region>.pooler.supabase.com:5432/postgres?sslmode=require
PORT=8080
